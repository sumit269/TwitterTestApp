<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ListItemImageViewStyle">
        <item name="android:layout_width">@dimen/listitem_text_left_padding</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:padding">@dimen/listitem_avatar_padding</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="ListItemTextViewStyle">
        <item name="android:paddingBottom">@dimen/listitem_text_bottom_padding</item>
        <item name="android:paddingLeft">@dimen/listitem_text_left_padding</item>
        <item name="android:paddingRight">@dimen/listitem_text_right_padding</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/listitem_text_size</item>
        <item name="android:typeface">normal</item>
    </style>

    <style name="ThreeLineListItemTextViewStyle" parent="ListItemTextViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">top</item>
        <item name="android:minLines">2</item>
        <item name="android:maxLines">3</item>
    </style>

    <style name="SpinnerStyle" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@null</item>
    </style>
</resources>
